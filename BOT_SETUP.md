# 🤖 Telegram Bot Setup

## Создание бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/botfather)
2. Отправьте команду `/newbot`
3. Введите имя бота (например: "3D Printer Calculator")
4. Введите username бота (например: "my3dprinter_bot")
5. Скопируйте токен бота (выглядит как: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

## Локальный запуск

### Windows:

```bash
# 1. Создайте файл .env
copy .env.example .env

# 2. Откройте .env и вставьте токен бота
notepad .env

# 3. Установите зависимости
pip install -r requirements.txt

# 4. Запустите бота
python bot.py
```

### Linux/Mac:

```bash
# 1. Создайте файл .env
cp .env.example .env

# 2. Откройте .env и вставьте токен бота
nano .env

# 3. Установите зависимости
pip install -r requirements.txt

# 4. Запустите бота
python bot.py
```

## Деплой на Render.com (бесплатно)

### Способ 1: Через GitHub (рекомендуется)

1. Запушьте код в GitHub:
```bash
git add .
git commit -m "Add Telegram bot"
git push
```

2. Зарегистрируйтесь на [Render.com](https://render.com)

3. Создайте новый **Web Service**:
   - Connect your GitHub repository
   - Name: `3d-printer-bot`
   - Environment: `Python 3`
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `python bot.py`

4. Добавьте переменные окружения (Environment Variables):
   - `BOT_TOKEN` = ваш токен от BotFather
   - `SUPABASE_URL` = (уже заполнен в коде)
   - `SUPABASE_KEY` = (уже заполнен в коде)

5. Нажмите **Create Web Service**

Бот запустится автоматически и будет работать 24/7!

### Способ 2: Через Render CLI

```bash
# Установите Render CLI
npm install -g render-cli

# Войдите в аккаунт
render login

# Создайте сервис
render service create --name 3d-printer-bot --env python --repo .

# Добавьте переменную окружения
render env set BOT_TOKEN your_token_here
```

## Деплой на Railway.app (бесплатно)

1. Зарегистрируйтесь на [Railway.app](https://railway.app)
2. Создайте новый проект из GitHub репозитория
3. Railway автоматически обнаружит Python проект
4. Добавьте переменную окружения `BOT_TOKEN`
5. Деплой запустится автоматически

## Деплой на Replit (самый простой)

1. Откройте [Replit.com](https://replit.com)
2. Создайте новый Repl → Import from GitHub
3. Вставьте ссылку на ваш репозиторий
4. В Secrets (замок слева) добавьте `BOT_TOKEN`
5. Нажмите Run

## Использование бота

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Используйте меню для:
   - Просмотра сводки по печатям
   - Добавления новых катушек пластика
   - Добавления печатей с автоматическим расчетом прибыли
   - Просмотра настроек принтера

## Возможности бота

- ✅ Добавление катушек пластика с расчетом цены за грамм
- ✅ Добавление печатей с полным расчетом:
  - Стоимость материала
  - Стоимость электричества
  - Амортизация оборудования
  - Себестоимость
  - Прибыль/убыток
- ✅ Сводка по всем печатям
- ✅ Синхронизация с Supabase
- ✅ Работает 24/7

## Обновление бота

Если вы внесли изменения в код:

1. Закоммитьте изменения:
```bash
git add .
git commit -m "Update bot"
git push
```

2. На Render.com/Railway бот автоматически обновится
3. На Replit нажмите Stop → Run

## Troubleshooting

**Бот не отвечает:**
- Проверьте что BOT_TOKEN правильный
- Проверьте логи на хостинге
- Убедитесь что сервис запущен

**Ошибка базы данных:**
- Проверьте что таблицы созданы в Supabase
- Проверьте что SUPABASE_URL и SUPABASE_KEY правильные

**Бот засыпает на Render (free tier):**
- Free tier засыпает после 15 минут бездействия
- Используйте сервис типа UptimeRobot для пинга каждые 5 минут
- Или перейдите на платный план ($7/мес)
