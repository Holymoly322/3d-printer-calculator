# 🔥 Инструкция по настройке Firebase

Следуйте этим шагам для настройки синхронизации данных между устройствами.

## Шаг 1: Создание проекта Firebase

1. Перейдите на [Firebase Console](https://console.firebase.google.com)
2. Нажмите "Add project" (Добавить проект)
3. Введите название проекта (например, "3d-printer-calc")
4. Отключите Google Analytics (не обязательно для этого проекта)
5. Нажмите "Create project"

## Шаг 2: Регистрация веб-приложения

1. В консоли Firebase выберите ваш проект
2. Нажмите на значок веб-приложения `</>` (Web)
3. Введите название приложения (например, "3D Printer Calculator")
4. **Не** ставьте галочку "Set up Firebase Hosting"
5. Нажмите "Register app"

## Шаг 3: Получение конфигурации

После регистрации вы увидите код с конфигурацией:

```javascript
const firebaseConfig = {
  apiKey: "AIzaSyXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789012",
  appId: "1:123456789012:web:xxxxxxxxxxxxx"
};
```

**Скопируйте эту конфигурацию!**

## Шаг 4: Включение Authentication

1. В боковом меню выберите "Build" → "Authentication"
2. Нажмите "Get started"
3. Выберите "Email/Password" в списке провайдеров
4. Включите **Email/Password** (первый переключатель)
5. Нажмите "Save"

## Шаг 5: Создание Firestore Database

1. В боковом меню выберите "Build" → "Firestore Database"
2. Нажмите "Create database"
3. Выберите режим запуска:
   - Выберите "Start in **production mode**"
   - Нажмите "Next"
4. Выберите регион (например, "europe-west1" для Европы)
5. Нажмите "Enable"

## Шаг 6: Настройка правил безопасности Firestore

1. В Firestore Database перейдите на вкладку "Rules"
2. Замените существующие правила на следующие:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

3. Нажмите "Publish"

**Что делают эти правила:**
- Каждый пользователь может читать и писать только свои данные
- Доступ разрешен только авторизованным пользователям
- Данные каждого пользователя изолированы

## Шаг 7: Обновление index.html

1. Откройте файл `index.html` в текстовом редакторе
2. Найдите строки 32-39 с конфигурацией:

```javascript
const firebaseConfig = {
    apiKey: "YOUR_API_KEY",
    authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
    projectId: "YOUR_PROJECT_ID",
    storageBucket: "YOUR_PROJECT_ID.appspot.com",
    messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
    appId: "YOUR_APP_ID"
};
```

3. Замените эту конфигурацию на **вашу** из Шага 3
4. Сохраните файл

## Шаг 8: Деплой на GitHub Pages

Если вы уже задеплоили приложение на GitHub Pages, просто закоммитьте и запушьте изменения:

```bash
git add index.html
git commit -m "Add Firebase configuration"
git push
```

Через несколько минут изменения появятся на вашем GitHub Pages.

## Шаг 9: Первый запуск

1. Откройте приложение в браузере
2. Вы увидите экран регистрации
3. Создайте аккаунт с вашим email и паролем
4. После регистрации вы автоматически войдете в приложение

## Теперь ваши данные синхронизируются! 🎉

- Войдите с одного устройства под вашим аккаунтом
- Добавьте катушки и печати
- Откройте приложение с другого устройства
- Войдите под тем же аккаунтом
- Все данные будут синхронизированы автоматически!

## Безопасность

✅ **Что безопасно:**
- Пароли хранятся в зашифрованном виде в Firebase
- Данные передаются по HTTPS
- Каждый пользователь видит только свои данные
- Firebase Authentication обеспечивает защиту

⚠️ **Рекомендации:**
- Используйте надежный пароль
- Не делитесь своими учетными данными
- Firebase бесплатный tier подходит для личного использования
- Для критичных бизнес-данных рассмотрите платный план

## Бесплатные лимиты Firebase

Бесплатный план Spark включает:
- 50,000 чтений в день
- 20,000 записей в день
- 1 GB хранилища
- Неограниченное количество пользователей

**Для личного использования этого более чем достаточно!**

## Troubleshooting

### Ошибка "Firebase: Error (auth/network-request-failed)"
- Проверьте интернет-соединение
- Убедитесь, что вы правильно скопировали конфигурацию

### Ошибка "Missing or insufficient permissions"
- Проверьте правила безопасности Firestore (Шаг 6)
- Убедитесь, что вы вошли в систему

### Данные не синхронизируются
- Проверьте, что вы вошли под одним и тем же email на всех устройствах
- Проверьте консоль браузера (F12) на наличие ошибок

---

**Нужна помощь?** Создайте Issue в репозитории GitHub.
